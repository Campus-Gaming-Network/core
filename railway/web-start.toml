[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/web-start/Dockerfile"
watchPatterns = [".dockerignore", "apps/docs/package.json", "apps/web/package.json", "apps/web-start/**", "package.json", "package-lock.json", "railway/web-start.toml"]

[deploy]
startCommand = "node src/production-preflight.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
drainingSeconds = "10"
